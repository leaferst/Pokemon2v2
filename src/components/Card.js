import React from 'react'

// picture, name, hp

function Card({pokemon, pokemonList}) {


// we want to compare pokemon to pokemonList
// then, we want to display the information from the unique url of the pokemon.

  console.log('this is card',pokemon);
  console.log(pokemonList);
  

  return (
    <div>Card</div>
  )
}

export default Card